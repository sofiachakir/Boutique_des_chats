<body class="bg-primary">
  <div class="container border rounded py-3 bg-white mt-5 col-7 mb-5">
    <div class="row d-flex align-items-center justify-content-center">
      <div class="col-md-6 text-center">
        <%= image_tag @item.picture, alt: 'picture', :class => "img-fluid rounded" %>
      </div>
      <div class="col-md-6">
        <br><br>

<h2>Modifier</h2>

      <%= form_for(@item, url: admin_item_path(@item), html: { method: :put }) do |f| %>

        <div class="field">
          <%= f.label :titre %><br />
          <%= f.text_field :title, class: 'form-control', autofocus: true, autocomplete: "title" %>
        </div>
        <br>
        <div class="field">
          <%= f.label :description %><br />
          <%= f.text_field :description, class: 'form-control', autofocus: true, autocomplete: "description" %>
        </div>
        <br>
        <div class="field">
          <%= f.label :prix %><br />
          <%= f.text_field :price, class: 'form-control', autofocus: true, autocomplete: "price" %>
        </div>
        <br>
      <br>
      <div class="actions">
          <%= f.submit "Mettre à jour", class: "btn btn-primary" %>
      </div>
       <% end %>
      <hr>
      <%= link_to "Retour", :back %>
      <br>
      <br>
      </div>
    </div>
  </div>
</body>

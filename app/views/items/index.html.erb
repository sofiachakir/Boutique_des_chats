<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="generator" content="Jekyll v4.0.1">
    <title>Test Boutique Chat</title>

</head>

<body>

  <div class="page-section clearfix">
    <div class="container">
      <div class="intro">
        <img class="intro-img img-fluid mb-3 mb-lg-0 rounded" src="https://images.unsplash.com/photo-1511275539165-cc46b1ee89bf?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=500&q=60" alt="">
        <div class="intro-text left-0 text-center bg-faded p-5 rounded">
          <h2 class="section-heading mb-4">
            <span class="section-heading-upper">Miauu..</span>
            <span class="section-heading-lower">Amoureux des chats</span>
          </h2>
          <p class="mb-3">Bienvenue sur votre boutique de photographies de chats. En pleine sieste, dans la nature ou en train de jouer, découvrez ces magnifiques photos de chats et chatons, touchants, mignons et craquants à souhait !
          </p>
          <br>
          <div class="intro-button mx-auto">
            <a class="btn btn-primary btn-xl" href="#">Découvrir</a>
          </div>
        </div>
      </div>
    </div>
  </div>

      <main role="main">
        <div class="album py-5 bg-light">
          <div class="container">
            <div class="card-columns">
              <% @items.order([:id]).each do |item| %>
                <% @id = Item.where(title: "#{item.title}").ids %>
                  <div class="">
                    <div class="card mb-4 custom-card">
                      <%= image_tag item.picture, alt: 'picture', :class => "bd-placeholder-img card-img-top" %>
                      <div class="card-body">
                        <p class="card-text"><%= item.title %></p>
                        <div class="d-flex justify-content-between align-items-center">
                          <div class="d-flex">
                            <a class="btn btn-sm btn-outline-primary" href="<%= item_path(item.id) %>" role="button">Voir</a>

                            <% if user_signed_in? %>
                              <%= link_to "Ajouter au panier", cart_item_join_index_path(item_id: item), method: :create, class: "btn btn-sm btn-outline-secondary ml-2" %>
                              <% else %>
                              <%= link_to "Ajouter au panier", new_user_session_path, class: "btn btn-sm btn-outline-secondary ml-2" %>
                            <% end %>

                          </div>
                          <small class="text-muted"><%= item.price %> €</small>
                        </div>
                      </div>
                    </div>
                  </div>
              <% end %>
            </div>
          </div>
        </div>
      </main>


    </body>
</html>


<div class="container mt-5 mb-5">
	<h1>Mon profil</h1>
	<p>Votre profil est lié à votre adresse email  : <%= current_user.email %></p>

	<%= link_to "Editer mon profil", edit_user_registration_path, class:"btn btn-secondary" %>


	<div class="container mt-5 mb-5">
		<div class="row">
			<div class="col-lg-12">
				<h2>Votre liste d'achats</h2>

				<% if @orders.empty? %>
					<p>Vous n'avez pas encore effectuer de commandes.</p>
					<img class="img-fluid rounded mx-auto d-block" src="https://images.unsplash.com/photo-1586223604555-7fdedc288185?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=1351&q=80" alt="cat"  style="width: 800px; height: auto;" >
				<% else %>
					<div class="bs-component">
						<% @orders.each do |order| %>
						<table class="table table-hover table-active">
  						<thead>
    						<tr>
      						<th scope="col">Titre</th>
      						<th scope="col">description</th>
      						<th scope="col">prix</th>
      						<th scope="col">Image(s)</th>
    						</tr>
  						</thead>

  						<tbody>
   			 				<% order.items.each do |item|%>
    							<tr class="table-light">
      							<th scope="row"><%= item.title %></th>
      							<td><%= item.description %></td>
      							<td><%= item.price %></td>
      							<td><img src="<%= item.image_url%>" class="img-thumbnail" alt="chat" width="60"></td>
									</tr>
									<br>
								<%end%>
  						</tbody>
						</table>
						<%end%> 
					</div>
				<% end %>

			</div>
		</div>
	</div>


</div>